var imageElement = document.querySelectorAll("img")

// var imgEl = document.querySelectorAll("img");
// * Give a`src` and`alt` attribute to the 3 provided`img` tags.
imageElement[0].setAttribute("src", "img1.jpeg")

imageElement[1].setAttribute("src", "img2.jpg")

imageElement[2].setAttribute("src", "img3.jpg")

// imgEl[0].setAttribute("src", "image_1.jpg");

  // * Give a`href` attribute to the 3 provided`a` tags.

  // * Add styles via`setAttribute` to make the page look decent.Recommended: margins and padding.